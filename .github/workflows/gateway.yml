name: gateway

on:
  push:
    branches: [main]
    paths:
      - 'gateway/**'
      - 'shared/**'
      - 'package.json'
      - 'package-lock.json'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build --workspace=gateway

      - name: Build image
        run: docker build -f gateway/Dockerfile -t mini-tube/gateway:${{ github.sha }} .

      - name: Push image
        run: echo "push mini-tube/gateway:${{ github.sha }} to registry"

      - name: Deploy
        run: echo "deploy gateway (e.g. docker compose pull && up, or kubectl set image)"
