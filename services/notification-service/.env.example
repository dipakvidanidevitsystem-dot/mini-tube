PORT=5017
JWT_SECRET=
CLIENT_URL=http://localhost:5173

# Shared secret for service-to-service calls into /internal/*; must match
# INTERNAL_API_KEY in the monolith / other services that publish events here.
INTERNAL_API_KEY=changeme-internal
