VITE_API_URL=http://localhost:5001/api

# Socket.IO now lives in its own notification-service (see migration plan step 2).
# Defaults to VITE_API_URL's host with /api stripped if unset — only needed once
# the notification-service runs on a different host/port than the API.
VITE_WS_URL=http://localhost:5017
